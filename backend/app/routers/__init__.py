"""HTTP layer.

Routers translate between HTTP and the service layer and do nothing else: no
grading, no gamification arithmetic, no unlock rules. If a handler grows an
``if``, that logic belongs in a service.
"""
