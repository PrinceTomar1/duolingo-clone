# Render blueprint for the backend.
#
# The live deployment runs here rather than on Railway because that account hit
# its free-plan resource cap. railway.json and the Procfile are kept alongside
# this and remain valid — the app is host-agnostic.
services:
  - type: web
    name: duolingo-clone-api
    runtime: python
    plan: free
    region: singapore
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    # `--if-empty` matters on a free instance: it sleeps after 15 minutes of
    # inactivity and re-runs this command on wake. An unconditional seed would
    # reset every learner's progress on each cold start.
    startCommand: >-
      alembic upgrade head &&
      python -m app.seed.seed_data --if-empty &&
      uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DEBUG
        value: "false"
      - key: DATABASE_URL
        fromDatabase:
          name: duolingo-clone-db
          property: connectionString
      - key: CORS_ORIGINS
        sync: false # set to the Vercel domain after the frontend deploys

databases:
  - name: duolingo-clone-db
    plan: free
    region: singapore
    postgresMajorVersion: "16"
